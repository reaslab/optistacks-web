# ReasAtlas framework sync 20260819 v1.2

This snapshot separates the first three A07 chapters into explicit subject
navigation entries:

- Derivative-Free Optimization (`A07.C01`): 582 topics, 1,738 statements;
- Manifold Optimization (`A07.C02`): 572 topics, 1,225 statements;
- Distributed Optimization (`A07.C03`): 543 topics, 1,089 statements.

The remaining A07 material stays available through Specialized Continuous and
Large-Scale Methods. All four entries resolve to the same canonical A07 data
source, so no topic or statement is copied.

The knowledge-card renderer now exposes structured mathematical fields that
were previously present in JSON but invisible: convergence targets and
objects, sequence/topology/probability/locality regimes, asymptotic rates,
nonasymptotic and epsilon-complexity bounds, variant dimensions, equivalent
formulations, boundary notes, relations, and statement-level evidence.

Review status, review flags, and pipeline/source-layer status are deliberately
not shown in the interface. The underlying provenance remains in the data.
