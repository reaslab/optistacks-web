# ReasAtlas framework sync 20260819 v1.1

This snapshot supersedes `20260819_framework_sync_v1` by exposing every
convergence candidate that had a mechanically validated artifact at the time
of the sync.

It contains two explicitly unreviewed convergence layers:

- 74 candidates from the completed 16-family core pass;
- 41 candidates from 12 PASS shards of the broader 128-shard pass.

The partial broader layer covers 12 A04 algorithm families and accounts for 43
admitted textbook evidence items exactly once. Sixteen generated records were
deferred by the convergence filter. One shard failed because its model stream
disconnected, and 115 shards had not started; neither category is exposed in
the website.

All 115 admitted records retain their assumptions, conclusion regime, rate or
complexity fields, boundary notes, variant relations, evidence locators, and
candidate review status. No canonical tree was mutated.
